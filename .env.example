# Backend Configuration
ASPNETCORE_ENVIRONMENT=Development
USE_IN_MEMORY_DATABASE=true

# Database (quando não usar InMemory)
# ConnectionStrings__DefaultConnection=Host=localhost;Database=portfolio;Username=seu_usuario;Password=sua_senha

# Frontend Configuration
VITE_API_BASE_URL=http://localhost:5167

# IMPORTANTE: Copie este arquivo para .env e preencha com seus valores reais
# NUNCA commite o arquivo .env com dados sensíveis!
